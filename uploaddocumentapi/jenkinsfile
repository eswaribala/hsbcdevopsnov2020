#!groovy
pipeline{
 agent any
 
  tools {
    maven 'maven'
  }
 
 stages{
 
   stage("build"){
   
       steps{
          echo "start building...." 
          bat "mvn -f ./uploaddocumentapi clean install -DskipTests"
          bat "docker build -f uploaddocumentapi/dockerfile -t uploaddocumentapp ./uploaddocumentapi"
          bat "docker run  -d uploaddocumentapp:latest"
                       
       }
   
   }
 
 }
 
 
 
}
